# Assets

Place static assets here — logos, illustrations, and images.

The app currently uses an inline SVG logo (see `AuthLayout` / `Sidebar`)
and `public/favicon.svg`, so no binary assets are required to run.
